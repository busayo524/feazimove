# ─── FeaziMove Frontend — Environment Variables ──────────────────────────────
# Copy this to .env.local — NEVER commit .env.local to git

# API base URL — points to your backend
VITE_API_URL=http://localhost:4000/api
